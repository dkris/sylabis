"""sylabis — the learning agent. Single source of truth for the version:
pyproject.toml reads it dynamically, `sy --version` prints it, and the MCP
server reports it in serverInfo."""

__version__ = "0.2.0"
